version: 2

sources:
  - name: jaffle_shop
    description: copy of PG db
    database: DEMO_DB
    tables:
      - name: customers
        description: raw cust data
        columns:
          - name: id
            description: PK
            tests:
              - unique
              - not_null
      - name: orders
        loaded_at_field: _etl_loaded_at
        freshness:
          warn_after: { count: 12, period: hour }
          error_after: { count: 240, period: hour }
        description: raw orders data
        columns:
          - name: id
            description: PK 
            tests:
              - unique
              - not_null